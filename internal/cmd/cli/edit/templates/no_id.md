You must specify the identifier of the object to edit. For example, to edit the cluster with
identifier `123`:

```shell
{{ binary }} edit cluster 123
```

Use the `--help` option to get more details about the command.
